{
  "Jwt": {
    // Development-only key so the project runs straight after clone. Any real deployment
    // must supply Jwt__SigningKey through user-secrets or an environment variable.
    "SigningKey": "dev-only-signing-key-change-me-in-production-0123456789"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
